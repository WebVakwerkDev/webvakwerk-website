FROM node:20-alpine AS runtime

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --omit=dev

COPY server ./server
RUN mkdir -p storage/ticket-attachments

EXPOSE 3001

CMD ["node", "server/portal-api.js"]
